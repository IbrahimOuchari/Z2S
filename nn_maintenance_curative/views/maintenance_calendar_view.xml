<odoo>
    <!-- 🎯 Calendar View Definition for maintenance.equipment -->
    <record id="view_maintenance_equipment_calendar" model="ir.ui.view">
        <field name="name">maintenance.equipment.calendar</field>
        <field name="model">maintenance.equipment</field>
        <field name="arch" type="xml">
            <calendar string="Vue Calendrier"
                      date_start="start_maintenance_date"
                      color="start_maintenance_date"
                      event_limit="5"
                      quick_add="false">
                <field name="technician_user_id" filters="1"/>
                <field name="next_maintenance_mensuelle" filters="1"/>
                <field name="next_maintenance_trimestrielle" filters="1"/>
                <field name="next_maintenance_semestrielle" filters="1"/>
                <field name="next_maintenance_annuelle" filters="1"/>
                <field name="name"/>
                <field name="category_id"/>
            </calendar>
        </field>
    </record>

    <!-- 📌 Update the action to include calendar in view_mode -->
    <record id="maintenance.hr_equipment_action" model="ir.actions.act_window">
        <field name="name">Équipements</field>
        <field name="res_model">maintenance.equipment</field>
        <field name="view_mode">kanban,tree,form,calendar</field>
        <field name="view_ids"
               eval="[(5, 0, 0), (0, 0, {'view_mode': 'calendar', 'view_id': ref('view_maintenance_equipment_calendar')})]"/>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Aucun équipement trouvé. Créez-en un pour commencer !
            </p>
            <p>
                Suivez vos équipements, assignez-les à un employé ou un département, et gérez leurs maintenances.
            </p>
        </field>
    </record>
</odoo>
