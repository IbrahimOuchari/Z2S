<odoo>
<odoo>
    <!-- Tree View for Type de Défaut -->
    <record id="view_type_defaut_tree" model="ir.ui.view">
        <field name="name">type.defaut.tree</field>
        <field name="model">defect2.type</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="description"/>
            </tree>
        </field>
    </record>

    <!-- Form View for Type de Défaut -->
    <record id="view_type_defaut_form" model="ir.ui.view">
        <field name="name">type.defaut.form</field>
        <field name="model">defect2.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="description"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action for Type de Défaut -->
    <record id="action_type_defaut2_list" model="ir.actions.act_window">
        <field name="name">Types de Défauts 2</field>
        <field name="res_model">defect2.type</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>

    <!-- Configuration Menu -->

    <!-- Reprise Menu Item -->

    <!-- Type de Défaut Menu Item -->
    <menuitem id="menu_type_defaut2" name="Types de Défauts 2" parent="menu_quality_control_config" sequence="30" action="action_type_defaut2_list" />
</odoo>
