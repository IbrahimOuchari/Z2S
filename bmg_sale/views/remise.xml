<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Affiche le montant de remise dans le récap BC Vente  -->
    <record id="view_remise" model="ir.ui.view">
        <field name="name">sale.order.form.remise</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref ="sale.view_order_form"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='note']" position="before">
                <group>
                <field name="general_discount" />
                    <field name ="discount_total"/>
                </group>
            </xpath>

        </field>
    </record>

    <!-- Appliqer remise totale compte client -->

    <record id="view_partner_form" model="ir.ui.view">
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="arch" type="xml">
            <xpath
                expr="//page[@name='sales_purchases']//field[@name='user_id']"
                position="after"
            >
                <label for="sale_discount" />
                <div>
                    <field name="sale_discount" class="oe_inline" /> %
                </div>
            </xpath>
        </field>
    </record>



</odoo>
