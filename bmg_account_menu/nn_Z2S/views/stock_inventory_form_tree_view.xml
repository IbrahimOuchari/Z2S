<odoo>

    <record id="view_stock_inventory_line_tree_custom" model="ir.ui.view">
        <field name="name">stock.inventory.line.tree.custom</field>
        <field name="model">stock.inventory.line</field>
        <field name="inherit_id" ref="stock.stock_inventory_line_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree" position="inside">
                <field name="category_id" string="Article Category"/>
                <field name="partner_id" string="Client"/> <!-- Change this line -->
                <field name="is_produit_fini" string="Produit Fini"/>
                <field name="is_produit_achete" string="Produit Acheté"/>
                <field name="is_produit_fourni" string="Produit Fourni"/>
            </xpath>
        </field>
    </record>

    <record id="view_inventory_form_custom" model="ir.ui.view">
        <field name="name">stock.inventory.form.custom</field>
        <field name="model">stock.inventory</field>
        <field name="inherit_id" ref="stock.view_inventory_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[@name='action_validate']" position="replace">
                <button name="action_validate" string="Validate Inven" type="object"
                        attrs="{'invisible': ['|', ('state', '!=', 'confirm'), ('line_ids', '=', [])]}"
                        class="oe_highlight" groups="nn_Z2S.stock_inventory_validation"/>

            </xpath>

            <!-- Add fields to the form view -->
            <xpath expr="//sheet" position="inside">
                <group>
                    <group string="Client Information">
                        <field name="category_id" attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        <field name="partner_id" attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                    </group>
                    <group string="Product Type Selection">
                        <field name="is_produit_fini" string="Produit Fini"
                               attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        <field name="is_produit_achete" string="Produit Acheté"
                               attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        <field name="is_produit_fourni" string="Produit Fourni"
                               attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                    </group>


                </group>
            </xpath>
            <xpath expr="//field[@name='accounting_date']" position="attributes">
                <attribute name="attrs">{'readonly': [('state', '!=', 'draft')]}</attribute>
            </xpath>
        </field>
    </record>
</odoo>
