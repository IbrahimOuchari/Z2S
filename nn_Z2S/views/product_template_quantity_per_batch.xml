<odoo>
    <record id="product_template_only_form_view" model="ir.ui.view">
        <field name="name">product.template.form.inherited</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <!-- Locate the description_sale field and add the new field after it -->
            <xpath expr="//page[1]/group/group[@name='group_general']/field[@name='description_sale']"
                   position="after">
                <field name="quantity_per_batch_default" string="Quantité par Lot"/>
            </xpath>
        </field>
    </record>

</odoo>
